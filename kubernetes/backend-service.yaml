apiVersion: v1
kind: Service
metadata:
  labels:
    app: voting-backend
  name: voting-backend
spec:
  ports:
  - port: 8080
    protocol: TCP
    targetPort: 8080
  selector:
    app: voting-backend
  type: ClusterIP
